{
    "name": "pcb-styler",
    "version": "1.0.96",
    "description": "Browser-based PCB styler with KiCad and Altium board support",
    "license": "AGPL-3.0-or-later",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "node src/server.mjs",
        "build:static": "node scripts/build-static-deploy.mjs",
        "test": "node --test tests",
        "format": "prettier --write .",
        "check:format": "prettier --check ."
    },
    "dependencies": {
        "express": "^5.1.0",
        "fflate": "^0.8.2",
        "altium-toolkit": "^0.1.15",
        "kicad-toolkit": "^0.2.13"
    },
    "devDependencies": {
        "prettier": "^3.4.2"
    },
    "engines": {
        "node": ">=20"
    }
}
